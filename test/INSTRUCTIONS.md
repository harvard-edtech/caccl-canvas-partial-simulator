Running init.js creates an instance of the Canvas partial simulator.

The following file is required:

test/config.js:

module.exports = {
  accessToken: 'put-the-token-of-the-canvas-user-here',
  canvasHost: 'canvas.school.edu',
  consumerKey: 'key',
  consumerSecret: 'secret',
};
